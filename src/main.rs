
// TODO Function that reads in keys/tokens
// bearer token might do, don't forget to strip the newlines

// TODO function that looks through accounts.json

struct Account {
    handle: String, // Includes "@"
    category: String,
}

fn main() {

    // Well, i gotta redo all the stuffs
    println!("Hello, world!");
}
